"""
CLI entrypoint per Strategy Auto Tuning (v1.0).

Responsabilità:
- parsing argomenti
- preflight input (KPI CSV, classifier/regime, config_strategy xlsx)
- invocazione engine (tuning per regime + selection)
"""
