"""
Costruzione dello spazio di ricerca (bounds) per i parametri ottimizzabili.

Responsabilità:
- derivare bounds da distribuzioni KPI su TRAIN (subset regime)
- definire discrete sets per enabled e shift (opzionale)
"""
