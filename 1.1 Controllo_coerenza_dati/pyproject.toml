[project]
name = "controllo-coerenza"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["pandas", "numpy"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
exclude = [".venv*", "__pycache__*"]
